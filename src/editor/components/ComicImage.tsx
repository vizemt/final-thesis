/*import { Sprite } from "pixi.js";

function ComicImage({ texture, x, y }) {
  return (
    <Sprite
      texture={texture}
      x={x}
      y={y}
      interactive
      pointerdown={(e) => console.log("select")}
    />
  )
}
  
Dragging images - Pixi supports pointer events.
<Sprite
  texture={texture}
  interactive
  pointerdown={startDrag}
  pointermove={drag}
  pointerup={endDrag}
/>

*/